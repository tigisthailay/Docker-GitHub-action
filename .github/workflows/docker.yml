name: Build and Push Docker Image to Docker Hub

on:
  push:
    branches:
      - main
env:
  # Use docker.io for Docker Hub if empty
  REGISTRY: docker.io
  # github.repository as <account>/<repo>
  IMAGE_NAME: ${{ github.repository }}

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      - name: Build the Docker image
        run: docker-compose build --no-cache --force-rm 
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Test the Docker image
        run: docker-compose up -d 
  push_to_registry:
    name: Push Docker image to Docker Hub
    runs-on: ubuntu-latest
    steps:
      - name: Check out the repo
        uses: actions/checkout@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Log in to Docker Hub
        uses: docker/login-action@f054a8b539a109f9f41c372932f1ae047eff08c9
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Extract metadata (tags, labels) for Docker
        id: meta
        uses: docker/metadata-action@98669ae865ea3cffbcbaa878cf57c20bbf1c6c38
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v2
        with:
          context: "{{defaultContext}}"
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}

# name: Build and Push Docker Image

# on:
#   push:
#     branches: [main]

# jobs:
#   build-and-push:
#     runs-on: ubuntu-latest
#     steps:
#     - name: Checkout code
#       uses: actions/checkout@v2

#     - name: Login to Docker Hub
#       uses: docker/login-action@v1
#       with:
#         username: ${{ secrets.DOCKER_USERNAME }}
#         password: ${{ secrets.DOCKER_PASSWORD }}

#     - name: Build Docker image
#       run: docker build -t tegisty13/github_docker_image:d2b_dockerImage

#     - name: Push Docker image to Docker Hub
#       run: docker push tegisty13/github_docker_image:d2b_dockerImage

# # name: Build and Push Docker Image
# # # description: Build and push Docker images
# # # author: Tegisty
# # # branding:
# # #   icon: 'anchor'
# # #   color: 'blue'

# # on:
# #   push:
# #     branches:
# #       - main

# # jobs:

# #   builds:

# #     runs-on: ubuntu-latest

# #     steps:
# #       # uses: actions/checkout@v2
# #       - name: Login to Docker Hub
# #         uses: docker/login-action@v1

# #         env:
# #           DOCKER_USER: ${{ secrets.DOCKER_USERNAME }}
# #           DOCKER_PASSWORD: ${{ secrets.DOCKER_PASSWORD }}
        
# #         run:
# #           docker login -u $DOCKER_USER -p $DOCKER_PASSWORD

# #       - name: BUILD the Docker Image
# #         run: docker build -t $DOCKER_USER/github_docker_image:d2b_dockerImage
# #         #run: docker build --file Dockerfile --tag $DOCKER_USER/github_docker_image:${{ github.sha }}
      
# #       - name: Docker Push
# #         run: docker push tegisty13/github_docker_image:d2b_dockerImage
